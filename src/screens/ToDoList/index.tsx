import React, { FC } from 'react';
import Navigator from '../../components/StackNavigation';

const ToDoListScreen:FC = () => {
    return (
        <Navigator/>
    )
}

export default ToDoListScreen;